extends RefCounted
## Modelo de credenciais OAuth do Google.
## Copie este arquivo para `google_oauth.gd` (mesma pasta) e preencha com as
## credenciais do seu projeto no Google Cloud Console:
##   APIs e servicos -> Credenciais -> ID do cliente OAuth (tipo "App para computador")
## O arquivo `google_oauth.gd` esta no .gitignore (nao vai pro versionamento).

const CLIENT_ID     := "SEU_CLIENT_ID.apps.googleusercontent.com"
const CLIENT_SECRET := "SEU_CLIENT_SECRET_GOCSPX"
